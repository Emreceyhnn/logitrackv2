========================================================================
 LOGITRACK v2 — PRELAUNCH TEMİZLİK / KALİTE YAPILACAKLAR
 Oluşturulma: 2026-07-06  |  Mevcut kalite notu: 82/100  |  Hedef: 90+
 Branch: Product-&-Business-Logic-Validation-(Logistics-Domain)
 Referans commit: 2ee3728
========================================================================

Not: Bu oturumda ŞUNLAR ZATEN YAPILDI (commit 2ee3728):
  [x] Driver regresyonu (geçersiz "AVAILABLE" status) düzeltildi
  [x] fix-anys.mjs throwaway script silindi
  [x] 247 kullanılmayan eslint-disable direktifi temizlendi (lint 0/0)
  [x] db.ts pg Pool globalThis singleton + DATABASE_URL fail-fast
  [x] redis.ts dosya-ortası import en üste taşındı
  [x] status.ts statusMap gerçek enum'a hizalandı (Record<DriverStatus,...>)


------------------------------------------------------------------------
 1) [YÜKSEK ETKİ] YARIM KALMIŞ REFACTOR'Ü BİTİR   (~ +8 puan)
------------------------------------------------------------------------
Sorun: İki soyutlama yaratıldı ama benimsenmesi eksik.

  a) controllerGuard adopsiyonu                          [x] TAMAMLANDI (kritik)
     - 46/50 kontrolör dosyası artık controllerGuard kullanıyor.
     - KALAN 4 dosya bilinçli olarak dışarıda bırakıldı: session/audit,
       session/core, session/manage, users/auth — bunlar auth/session'a özgü
       özel hata işleme (custom mesaj, audit-log yutma, graceful return)
       içeriyor; mekanik guard'a sokmak riskli.
     - !! ÖNEMLİ FIX: Toplu script (apply-guard-refactor) graceful-degradation
       catch'lerini (catch { return []/null/{...} }) körlemesine rethrow eden
       guard'a çevirmişti -> dashboard widget'ları hata anında çöküyordu
       (reports testi kırılmıştı). controllerGuard'a opsiyonel { fallback }
       eklendi ve 22 read fonksiyonuna geri uygulandı (fleet/operations/
       shipments/warehouse analytics, overview, reports, routes/stats,
       shipments/stats, vehicle/issues, warehouse/stats, map). Davranış
       eski sözleşmeye hizalandı.
     - Ayrıca script'in "operation" placeholder bıraktığı 6 op adı gerçek
       fonksiyon adlarıyla düzeltildi.

  b) createCacheManager adopsiyonu                        [~] ERTELENDİ (opsiyonel, launch-dışı)
     - Şu an 8 dosya: customer, driver/shared, trailer, routes/cache,
       shipments/cache, vehicle/cache, warehouse/cache, utils/cacheFactory.
     - Kalan bazı yerler hâlâ manuel withCache + createCacheKeys kullanıyor
       (inventory, company, overview, *//queries.ts). Bloklayıcı değil.
     - HEDEF: her domain için tek bir cache manager instance.
     - KARAR (2026-07-07): launch öncesi YAPILMIYOR. redis.ts zaten tüm anahtar
       setlerini createCacheKeys ile üretiyor; manuel kontrolörler aynı anahtarları
       withCache ile kullanıyor. Manager'a geçmek BİREBİR aynı anahtarları üreten
       saf stil konsolidasyonu (düşük değer). Ayrıca overview (özel dashboard key +
       300s TTL), inventory/company/trailer (createCacheKeys üzerine override edilmiş
       ekstra anahtarlar) manager'a temiz oturmuyor. Testler redis'i mock'ladığından
       anahtar-format regresyonunu YAKALAMAZ -> launch öncesi risk/ödül düşük.

  YÖNTEM (ÖNEMLİ - launch riski): dosya-dosya / domain-domain yap,
  HER grup sonrası ilgili testi çalıştır. Toptan değiştirip tek seferde
  commit etme. (Bu ders yukarıdaki toplu-script regresyonuyla doğrulandı.)


------------------------------------------------------------------------
 2) HATA MESAJI & DİL STANDARDİZASYONU                     [~] KISMİ / ERTELENDİ
------------------------------------------------------------------------
  - Kod içi mesajlar karışık: bazı throw new Error(...) İngilizce,
    bildirim (notification) metinleri Türkçe, yorumlar Türkçe-İngilizce karışık.
  - HEDEF:
      * Domain/teknik hatalar -> İngilizce sabit (veya ErrorCode enum).
      * Kullanıcıya giden her metin -> i18n sözlüğü üzerinden (tr.json/en.json).
      * Kod yorumları için tek dil seç (öneri: İngilizce) ve tutarlı ol.
  - DURUM (2026-07-07):
      * throw new Error(...) mesajları ZATEN İngilizce (controllers'da 117 adet,
        0'ında Türkçe karakter) -> teknik hata dili tutarlı. ✓
      * KALAN: ~19 kontrolör dosyasında kullanıcıya giden Türkçe BİLDİRİM metni
        (notification title/message: documents, driver/crud, driver/status,
        maintenance, routes/assignments, ...). Bunlar server-side üretilip DB'ye
        yazılıyor; doğru düzeltme = i18n key+params saklayıp alıcının diline göre
        render (locale-aware notification) -> mimari değişiklik.
      * KARAR: launch öncesi YAPILMIYOR (invaziv, orta risk). Launch-sonrası
        bildirim i18n epic'ine bırakıldı.


------------------------------------------------------------------------
 3) REDIS PROD FAIL-FAST                                      [x] TAMAMLANDI
------------------------------------------------------------------------
  Dosya: app/lib/redis.ts
  YAPILDI:
    - redisRequired = CI || NODE_ENV==="production"; bu ortamlarda url VEYA token
      eksikse fail-fast throw (artık token de kontrol ediliyor, sadece url değil).
    - "dummy" fallback yalnızca lokal dev/test'e bırakıldı.
    - BONUS: refactor'ün düşürdüğü trailer cache "list_v2" sürüm-bump'ı geri
      getirildi (createCacheKeys override) — redis.test 24/24 yeşil.


------------------------------------------------------------------------
 4) TESTLERİ DOMAIN INVARIANT'LARINA BAĞLA               [x] TAMAMLANDI (kısmi)
------------------------------------------------------------------------
  YAPILDI: 9 controller testinde (driver, shipments, session, maintenance,
    fuel, routes, overview, reports, warehouseWorker) ham enum string
    literal'leri @prisma/client enum sabitlerine çevrildi
    (örn status: "OFF_DUTY" -> DriverStatus.OFF_DUTY). Artık geçersiz enum
    üyesi (AVAILABLE gibi) DERLENMEZ. tsc + 54 test yeşil.
  BONUS: maintenance.test'te başka bir refactor regresyonu yakalandı — eski
    "Invalid vehicle..." mesajı beklentisi, standart NotFoundError("Vehicle")
    mesajına ("Vehicle not found or unauthorized") hizalandı.
  KALAN (opsiyonel): Diğer test dosyalarındaki (api routes, hooks, components)
    enum literal'leri; ve controller import'larını `any` yerine gerçek tiple
    tiplemek (argümanların tamamı derleme-zamanı kontrol edilir). Daha invaziv.
  AYRI KÜÇÜK BULGU: [x] WarehouseTaskStatus client-safe enum aynasına
    (app/lib/type/enums/enums.ts) eklendi (OPEN/IN_PROGRESS/COMPLETED),
    schema ile birebir. tsc + lint yeşil.


------------------------------------------------------------------------
 5) PRISMA MIGRATE / DEPLOY DOĞRULAMASI (pg adapter geçişi)   [x] TAMAMLANDI
------------------------------------------------------------------------
  Değişiklik: schema.prisma datasource'tan url kaldırıldı; artık pg driver
              adapter + prisma.config.ts (env DATABASE_URL) kullanılıyor.
  DOĞRULANDI:
    * prisma validate        -> config prisma.config.ts'ten yükleniyor, schema geçerli ✓
    * prisma generate        -> Prisma Client v7.8.0 üretildi ✓
    * prisma migrate status  -> Neon DB'ye bağlanıyor, 4 migration, schema güncel ✓
    * npm run build          -> exit 0 (prisma generate && eslint && next build) ✓
  ÖNEMLİ BULGU + FIX (db.ts):
    - #4'teki getPool() fail-fast'i DATABASE_URL yoksa IMPORT anında çöküp
      next build'i kırma riski taşıyordu (build script .env yüklemiyor, shell'de
      DATABASE_URL yok). Guard eklendi: build fazında (NEXT_PHASE===
      "phase-production-build") throw etmiyor, pg lazy bağlanıyor; runtime'da
      hâlâ fail-fast. build DATABASE_URL olmadan da geçiyor (SSG DB sorgulamıyor).
  NOT (platform): CI/prod'da migrate/deploy için DATABASE_URL ortam değişkeni
       yüklü olmalı (build DB'siz geçse de runtime ve migrate DB ister).


------------------------------------------------------------------------
 6) KÜÇÜK / OPSİYONEL CİLA
------------------------------------------------------------------------
  - [x] RTL component testleri yavaşlığı: run-tests.mjs unit (.test.ts) ve
    component (.test.tsx) testlerini ayırıp, jsdom'u her dosyada tekrar kurmak
    yerine tek sefer paylaşımlı --import ile yüklüyor (scripts/test-setup-dom.mjs
    + test-setup-css.mjs CSS import stub). Her .test.tsx'ten tekrarlı
    "global-jsdom/register" importları kaldırıldı. Tek seferlik refactor
    script'leri (remove-jsdom-imports, apply-guard-refactor, refactor-controllers)
    silindi.
  - [x] BONUS: toplu-refactor sonrası 4 kırık test düzeltildi — trailer conflict
    mesajı (ConflictError, cümle-düzeni) test'e hizalandı; inventory testi
    geçersiz "OUTBOUND" MovementType yerine "PICK" kullanacak şekilde;
    checkPermission testi gerçek NoCompanyError mesajına ("User has no company");
    useDateSettings testi eksik useLanguage/DictionaryContext mock'u eklenerek.

  - [x] KOMPONENT TESTİ İZOLASYON BORCU (önemli, önceden mevcut): ÇÖZÜLDÜ.
    DOĞRULAMA (2026-07-07): tam suite yeşil — 120/120 unit + 165/165 component,
    0 hata. tsc --noEmit exit 0; eslint --max-warnings=0 exit 0. Aşağıda "KALAN"
    diye listelenen ~13 dosya artık her biri KENDİ sürecinde (izole runner)
    geçiyor; mock sızıntısına bağımlılık kalmadı. Tarihsel not olarak bırakıldı:
    BULGU: `npm test` HİÇBİR ZAMAN tam yeşil değildi — ilk ölçümde ~31 component
    testi kırıktı. Sebep: node:test `mock.module` kayıtları süreç-kapsamlı ve
    aynı partideki dosyalar arasında SIZIYOR. readdirSync sırası sabit olduğundan
    parti sonuçları deterministik ama bazı page/content/dialog testleri yalnızca
    bir kardeş dosya ihtiyaç duydukları mock'u (QueryClient, DictionaryContext,
    useUser, react hook'ları) sızdırdığı için geçiyor; tek başına koşunca kırılıyor.
    YAPILDI:
      * 9 dashboard page.test.tsx (company/customers/drivers/inventory/overview/
        reports/routes/vehicle/warehouses): render() artık QueryClientProvider ile
        sarılıyor (HydrationBoundary useQueryClient istiyor). Tek başına yeşil.
      * editCustomerDialog + SecurityTab testleri kendi mock/state'lerini kuracak
        şekilde bağımsız hale getirildi.
    KALAN (~13 dosya, her biri kendine özgü, önceden kırık — bespoke iş):
      * "testid bulunamadı": analytics/ReportsContent/DriverContent (eksik child
        component mock'u).
      * eksik/yanlış mock: customTextArea + sidebar (react hook'ları/createContext),
        NotificationBell (DictionaryContext.useLanguage), landing/layout (dict).
      * assertion: loading (sayaç>10), landing/page (sayı=4).
      * tema: landing/pricing (MUI augmentColor "success" tanımsız).
      * param: (dashboard)/layout (params.lang undefined).
      * YARIM/EKSİK yazılmış: CustomerContent + WarehouseContent testleri tanımsız
        değişkenlere (mockRefetch, mockDeleteMutateAsync) ve eksik hook/child
        mock'larına atıf yapıyor — tamamlanması test YAZMA işi.
    ÖNERİ (profesyonel hedef): component testlerini süreç-başına-dosya izolasyonuyla
    koş (mock sızıntısını imkânsız kılar, deterministik) VE her testi kendi
    kendine yeterli yap. İzole runner taslağı denendi (parti yerine paralel havuz);
    çalışıyor ama kalan ~13 dosya kendi mock'larını kurana kadar bu dosyalar
    kırmızı kalır. Şimdilik user'ın parti-runner'ı korundu (aynı kırık taban).
  - [x] CRLF/LF uyarıları: .gitattributes eklendi (* text=auto eol=lf + binary
    + lockfile). `git add --renormalize .` 0 içerik değişikliği (repo zaten LF).
    "LF will be replaced by CRLF" gürültüsü bitti. (commit 19aaed6)
  - [x] lint-staged: "*.{ts,tsx}" -> "eslint --max-warnings=0" olarak
    sıkılaştırıldı; yalnızca staged dosyalarda çalışıyor (teyit edildi:
    lint-staged staged eşleşme yoksa geçiyor). 0-uyarı durumu kilitlendi. (19aaed6)


========================================================================
 DURUM (2026-07-07) — LAUNCH KAPISI:
   [x] #1a controllerGuard  [x] #3 redis  [x] #4 test enum  [x] #5 migrate
   [x] #6 komponent test izolasyonu (tam suite YEŞİL: 120 unit + 165 component,
       0 hata; tsc 0; eslint --max-warnings=0 0)
   [~] #1b cache manager  — ERTELENDİ (opsiyonel, saf stil, launch-dışı)
   [~] #2  dil std.        — teknik hatalar İngilizce ✓; bildirim i18n ERTELENDİ
   -> LAUNCH-BLOKLAYICI HİÇBİR MADDE KALMADI. Kalanlar bilinçli opsiyonel/erteleme.

 NOT: Çalışma ağacında #6'yı yeşile taşıyan değişiklikler (test izolasyon
      runner'ı + self-yeterli mock'lar) commit'e HAZIR ama henüz commit EDİLMEDİ.
========================================================================
